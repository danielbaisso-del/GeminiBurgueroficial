## Production environment (example, NON-SENSITIVE placeholders)
# Copy to .env.production and replace placeholders with real values stored in a secrets manager.

NODE_ENV=production

# Managed MySQL (example host)
DATABASE_URL="mysql://<db_user>:<db_password>@<db_host>:3306/<db_name>"

JWT_SECRET="use-a-secrets-manager-to-store-this-secret"
JWT_EXPIRES_IN="7d"

PORT=3333
FRONTEND_URL="https://app.your-domain.com"

CORS_ORIGIN="https://app.your-domain.com"

RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=200

GEMINI_API_KEY=""

EMAIL_HOST="smtp.sendprovider.example"
EMAIL_PORT=587
EMAIL_USER="no-reply@your-domain.com"
EMAIL_PASS=""

WHATSAPP_API_URL="https://api.your-whatsapp-provider.example"
WHATSAPP_API_TOKEN=""

MERCADOPAGO_ACCESS_TOKEN=""
STRIPE_SECRET_KEY=""

# Notes:
# - Replace placeholders before deploying. Do NOT commit real secrets.
# - Prefer injecting secrets via your CI/CD or hosting provider secret store.
## Production environment example for Gemini Burger backend
# Copy this file to .env.production and replace placeholders with real secrets.
# DO NOT commit real secrets to git.

# Environment
NODE_ENV=production

# Database (use a managed DB for production)
# Format: mysql://user:password@host:3306/database
DATABASE_URL="mysql://<db_user>:<db_password>@<db_host>:3306/<db_name>"

# JWT - generate a strong random secret (64 bytes hex)
# Example: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="replace-with-production-jwt-secret"
JWT_EXPIRES_IN="7d"

# Server
PORT=3333
FRONTEND_URL="https://your-frontend.example.com"

# CORS - comma-separated allowed origins
CORS_ORIGIN="https://your-frontend.example.com"

# Rate limiting (ms window, max requests per window)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=200

# Gemini AI (optional)
GEMINI_API_KEY=""

# Email (optional)
EMAIL_HOST=""
EMAIL_PORT=587
EMAIL_USER=""
EMAIL_PASS=""

# WhatsApp Business API (optional)
WHATSAPP_API_URL=""
WHATSAPP_API_TOKEN=""

# Payment Gateways - configure according to provider
MERCADOPAGO_ACCESS_TOKEN=""
STRIPE_SECRET_KEY=""

# SECURITY NOTES:
# - Use HTTPS termination (load balancer) in front of the app.
# - Restrict DB access to application IPs.
# - Rotate secrets regularly and store them in a secrets manager (AWS Secrets Manager, Azure Key Vault, or similar).
# Remove local-sensitive examples. Use placeholders and secrets management.
NODE_ENV=production

DATABASE_URL="mysql://<db_user>:<db_password>@<db_host>:3306/<db_name>"

JWT_SECRET="replace-with-production-jwt-secret"
JWT_EXPIRES_IN="7d"

PORT=3333
FRONTEND_URL="https://your-frontend.example.com"

CORS_ORIGIN="https://your-frontend.example.com"

RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=200

GEMINI_API_KEY=""

EMAIL_HOST=""
EMAIL_PORT=587
EMAIL_USER=""
EMAIL_PASS=""

WHATSAPP_API_URL=""
WHATSAPP_API_TOKEN=""

MERCADOPAGO_ACCESS_TOKEN=""
STRIPE_SECRET_KEY=""
