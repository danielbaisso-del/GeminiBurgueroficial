# Environment (development, production)
NODE_ENV=development

# Database
# IMPORTANTE: Troque user, password e gemini_burger pelos seus dados reais
DATABASE_URL="mysql://user:password@localhost:3306/gemini_burger"

# JWT - ATENÇÃO: Gere um secret forte em produção!
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production-use-crypto-random-64-bytes"
JWT_EXPIRES_IN="7d"

# Server
PORT=3333
FRONTEND_URL="http://localhost:5173"

# CORS - Adicione domínios permitidos em produção
CORS_ORIGIN="http://localhost:5173,http://localhost:3000"

# Rate Limiting (requisições por minuto)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Gemini AI (opcional - pode usar a key do tenant)
GEMINI_API_KEY="your-gemini-api-key"

# Email (para notificações - opcional)
EMAIL_HOST="smtp.example.com"
EMAIL_PORT=587
EMAIL_USER="your-email@example.com"
EMAIL_PASS="your-email-password"

# WhatsApp Business API (opcional)
WHATSAPP_API_URL=""
WHATSAPP_API_TOKEN=""

# Payment Gateway (adicione quando integrar)
MERCADOPAGO_ACCESS_TOKEN=""
STRIPE_SECRET_KEY=""
